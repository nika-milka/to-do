name: CI Pipeline

on:
  pull_request:
    branches: [dev]
  push:
    branches: [dev]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout        echo \" CI Pipeline completed successfully\"
        
    - name: Simple test
      run: |
        if [ -f ci_test.py ]; then
          echo \"Running ci_test.py:\"
          python ci_test.py
        else
          echo \"No ci_test.py found\"
        fi
